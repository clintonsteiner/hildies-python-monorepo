# Go builds are handled via go build (see ../go.mod)
# To build: go build -o hildie-go-app ./

filegroup(
    name = "hildie-go-app",
    srcs = glob(["*.go"]),
    visibility = ["//visibility:public"],
)
